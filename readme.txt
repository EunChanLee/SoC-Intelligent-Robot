사용 로봇: 로보티즈(사) 바이올로이드 GP
amazon2_sdk.h // SoC 내부 FPGA jtag와 통신에 필요한 코드
graphic_api.c,h // 영상 읽고 SoC 보드 앞면 화면에 영상 출력하는 등의 코드
uart_api.c,h // 로봇 제어기 CM-530과 UART통신에 필요한 코드
stdip_ex.c,h // 로봇의 영상 인식 및 판단하는 함수
mt_driver.c,h // 로봇의 행동을 결정하는 함수
new_main.c // 메인 함수
robot_protocol.c,h // 패킷전송함수, 지연함수, 로봇<->보드 통신함수
